# Teacher Lesson Tracker

A local-first React application for lesson planning, schedules, Course Maps, class progress and academic-calendar management.

## Local development

```bash
npm install
npm run dev
```

On Windows, `start.cmd` can be used to launch the development server.

## Checks

```bash
npm test
npm run build
```

Application data is stored locally in the browser using `localStorage`.
