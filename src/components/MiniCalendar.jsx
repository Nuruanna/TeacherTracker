import { useEffect,useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { DEMO_DATE, isoDate } from '../utils/date';
import leafDecoration from '../../assets/leaf-decoration.png';
const DAYS=['MON','TUE','WED','THU','FRI','SAT','SUN'];
export default function MiniCalendar({selectedDate=DEMO_DATE,onSelect}){const navigate=useNavigate();const [visibleMonth,setVisibleMonth]=useState(()=>new Date(selectedDate.getFullYear(),selectedDate.getMonth(),1));useEffect(()=>setVisibleMonth(new Date(selectedDate.getFullYear(),selectedDate.getMonth(),1)),[selectedDate.getFullYear(),selectedDate.getMonth()]);const year=visibleMonth.getFullYear(),month=visibleMonth.getMonth();const first=new Date(year,month,1);const leading=(first.getDay()+6)%7;const cells=Array.from({length:42},(_,i)=>new Date(year,month,i-leading+1));const title=new Intl.DateTimeFormat('en-GB',{month:'long',year:'numeric'}).format(visibleMonth);const select=date=>onSelect?onSelect(date):navigate(`/day?date=${isoDate(date)}`);const changeMonth=amount=>setVisibleMonth(current=>new Date(current.getFullYear(),current.getMonth()+amount,1));return <section className="mini-calendar card"><div className="calendar-head"><button aria-label="Previous month" onClick={()=>changeMonth(-1)}>‹</button><h2>{title}</h2><button aria-label="Next month" onClick={()=>changeMonth(1)}>›</button></div><div className="calendar-grid">{DAYS.map(d=><b key={d}>{d}</b>)}{cells.map((date,i)=>{const muted=date.getMonth()!==month;const current=isoDate(date)===isoDate(selectedDate);return <button key={i} className={`${muted?'muted ':''}${current?'current':''}`} onClick={()=>select(date)}>{date.getDate()}</button>})}</div><img className="calendar-leaf" src={leafDecoration} alt=""/></section>}
