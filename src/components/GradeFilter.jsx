import { teachingGroupFilterOptions } from '../services/teachingGroupService';
import { getAppTodayISO } from '../utils/appTime';
export default function GradeFilter({teachingGroups,value,onChange,date=getAppTodayISO()}){const options=teachingGroupFilterOptions(teachingGroups,date);return <label className="grade-filter"><span className="sr-only">Filter lessons by grade</span><select value={value} onChange={event=>onChange(event.target.value)}>{options.map(option=><option key={option.value} value={option.value}>{option.label}</option>)}</select><svg className="grade-filter-chevron" aria-hidden="true" viewBox="0 0 16 16"><path d="M3 5.5 8 10.5l5-5"/></svg></label>}
